trait Fibbable<T> {
	fn get(n: T) -> T;
}

struct Fib;

impl Fibbable<T: Num> for Fib {
	fn get(n: T)
}

fn fib(n: i32) -> i32 {
	match n {
		0 | 1 => n,
		_ => fib(n-1) + fib(n-2)
	}
}

fn main() {
	for i in 0..40 {
    	println!("fib({}) = {}", i, fib(i));
    	
   	}
}